package com.example.smple.ui.auth

import androidx.compose.runtime.Composable

@Composable
fun LoginScreen(
    viewModel: AuthViewModel,
    onLoginSuccess: () -> Unit,
    onSignUpClick: () -> Unit,
    onForgotPasswordClick: () -> Unit,
) {
    // TODO: Email + password fields, Login button, nav links
}
