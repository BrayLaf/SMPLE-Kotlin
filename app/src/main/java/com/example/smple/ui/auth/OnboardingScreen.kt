package com.example.smple.ui.auth

import androidx.compose.runtime.Composable

@Composable
fun OnboardingScreen(
    onSignUpClick: () -> Unit,
    onLoginClick: () -> Unit,
) {
    // TODO: Display SMPLE logo, tagline, Sign Up / Login buttons
}
