package com.example.smple.ui.auth

import androidx.compose.runtime.Composable

@Composable
fun SignUpScreen(
    viewModel: AuthViewModel,
    onSignUpSuccess: () -> Unit,
    onLoginClick: () -> Unit,
) {
    // TODO: Email + password + confirm password fields, Sign Up button
}
