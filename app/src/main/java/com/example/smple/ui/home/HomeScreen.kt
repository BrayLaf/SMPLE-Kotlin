package com.example.smple.ui.home

import androidx.compose.runtime.Composable

@Composable
fun HomeScreen(
    viewModel: HomeViewModel,
    onEntryClick: (Int) -> Unit,
) {
    // TODO: Monthly calendar with highlighted training days
    // TODO: List of recent workout entries below calendar
}
